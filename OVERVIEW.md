# 아주소중한딥러닝챌린지_2026

## Overview
범용 LLM(Qwen2.5-3B-Instruct)을 활용하여 처음 보는 수학 문제의 정수 답을 정확히 추론하는 모델을 만드는 대회입니다.

---

## Description

'아주 소중한 딥러닝 챌린지 2026'에 오신 것을 환영합니다. 🎉🎉

본 대회는 LLM의 수학적 추론 능력의 한계를 탐색하고, 이를 극대화할 수 있는 최적의 학습 전략을 발굴하고자 개최되었습니다.

이번 대회의 목표는 수학에 특화되지 않은 하나의 범용 모델(Qwen2.5-3B-Instruct)을 출발점으로, 다양한 기법을 활용하여 처음 보는 수학 문제를 정확히 풀 수 있는 추론 능력을 갖춘 모델로 발전시키는 것입니다.

---

## 주요 규칙 📌
원활한 대회 진행을 위해 모든 참가자는 아래의 핵심 규칙을 준수해야 합니다.

### 고정 모델 (Fixed Base Model)
모든 참가자는 동일한 하나의 베이스 모델에서 출발해야 합니다.
- 베이스 모델: Qwen/Qwen2.5-3B-Instruct (HuggingFace)
- 다른 모델(Qwen2.5-Math, DeepSeek-R1, Llama 등)을 베이스로 사용하는 것은 금지됩니다.

### 데이터 사용
- 주최 측이 제공하는 학습 데이터를 기본으로 활용합니다.
- 공개 데이터 셋등 추가 외부 데이터 사용은 자유입니다.
- 상용 API로 테스트 문제의 답을 직접 생성하는 것은 금지입니다.

### 추론 환경
- 추론 시 인터넷 접속 금지(외부 API 호출 불가)
- 추론 시점의 기법(Majority Voting, Self-Consistency 등)은 자유롭게 사용할 수 있습니다.

---

## 평가 (Evaluation) ⚖️
대회 평가는 실시간 리더보드와 최종 리더보드 두 가지로 운영됩니다. 최종 순위는 최종 리더보드의 점수를 기준으로 결정됩니다.

### 실시간 리더보드 (중간 순위)
대회 기간 동안 모델의 성능을 빠르게 확인하고 개선 방향을 잡기 위한 참고용 리더보드입니다.

- 평가 시점: 대회 기간 중 (실시간)
- 사용 데이터: test.parquet의 일부(Public Test)를 사용하여 채점합니다.
- 평가 지표: Accuracy (Exact Match)

이 점수는 최종 순위에 영향을 미치지 않습니다.

### 최종 리더보드 (최종 순위)
대회 종료 후, 전체 Test 데이터에 대한 모델의 최종 성능을 평가하고 공식 순위를 결정하기 위한 리더보드입니다.

- 평가 시점: 대회 종료 후 최종 평가
- 사용 데이터: test.parquet의 나머지(Private Test)를 사용하여 채점합니다.
- 평가 지표: Accuracy (Exact Match)

### 채점 방식
- 모든 문제의 정답은 정수입니다
- 제출된 정수와 정답이 정확히 일치하면 정답, 그렇지 않으면 오답으로 판정합니다.
⚠️ 모델의 출력에는 풀이 과정, 수식, 자연어 설명 등이 포함될 수 있습니다. 참가자는 모델 출력에서 최종 답을 추출하는 후처리(post-processing)를 직접 수행하여, submission.csv의 answer 컬럼에는 반드시 정수만 기입해야 합니다.

### 제출 파일 형식 (Submission File Format)
- 파일명: submission.csv
- 파일 구조: ID와 answer 두 개의 컬럼

**ID** | **answer** |
prob_0001 | 42 |
prob_0002 | 7 |
prob_0003 | 125 |

---

## 대회 일정
일정 | 날짜 |
모집 기간 | 2026.06.08. ~ 06.28. |
참여 확정 | 2026.07.20. ~ 07.27. |
문제 공개 | 2026.07.31. (예정) |
이론 학습 | 2026.07.31. ~ 08.30. |
챌린지 | 2026.07.31. ~ 08.30. |
평가 및 검증 | 2026.08.01. ~ 09.20. (예정) |
수상자 발표 | 2026.09.28. (예정) |

---

## 발표
최종 12팀은 발표가 있을 예정입니다.
모델 성능(50%) + 발표 평가(모델 우수성)50% 12팀(명) 검증 후 순위에 따라 최종 9팀(명)이 수상 대상자가 됩니다
발표: 발표 날짜 추후 공지 예정
